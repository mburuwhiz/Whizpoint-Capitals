<div style="max-width: 400px; margin: 4rem auto;">
  <div class="text-center mb-8">
    <div style="font-size: 3rem; color: var(--primary); margin-bottom: 1rem;">🔒</div>
    <h2 style="font-size: 2rem; font-weight: 800;">New Password</h2>
    <p class="text-muted">Please enter your new password below.</p>
  </div>

  <div class="card fade-in">
    <form action="/reset-password/<%= token %>" method="POST" style="display: flex; flex-direction: column; gap: 1.5rem;">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <div>
        <label class="font-bold" style="font-size: 0.875rem;">New Password</label>
        <div style="position: relative;">
          <input id="password" name="password" type="password" required class="input-field" placeholder="••••••••">
          <button type="button" id="togglePassword" style="position: absolute; right: 1rem; top: 50%; transform: translateY(-50%); background: none; border: none; cursor: pointer; color: var(--text-muted);">
            👁️
          </button>
        </div>
      </div>

      <button type="submit" class="btn btn-primary" style="padding: 1rem; font-size: 1rem;">
        Set New Password
      </button>
    </form>
  </div>
</div>

<script>
  const togglePassword = document.querySelector('#togglePassword');
  const password = document.querySelector('#password');

  togglePassword.addEventListener('click', function (e) {
    const type = password.getAttribute('type') === 'password' ? 'text' : 'password';
    password.setAttribute('type', type);
    this.textContent = type === 'password' ? '👁️' : '🕶️';
  });
</script>
