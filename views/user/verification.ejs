<div class="max-w-4xl mx-auto">
  <div class="mb-8">
    <h1 class="text-3xl font-extrabold text-gray-900 dark:text-white">Account Verification</h1>
    <p class="text-muted">Verify your identity to unlock higher limits and full withdrawal capabilities.</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
    <div class="md:col-span-1 space-y-6">
      <nav class="card p-0 overflow-hidden">
        <a href="/profile" class="block px-6 py-4 text-gray-600 hover:bg-gray-50 border-l-4 border-transparent transition">
          <i class="fas fa-user-circle mr-2"></i> General
        </a>
        <a href="/profile/notifications" class="block px-6 py-4 text-gray-600 hover:bg-gray-50 border-l-4 border-transparent transition">
          <i class="fas fa-bell mr-2"></i> Notifications
        </a>
        <a href="/profile/disputes" class="block px-6 py-4 text-gray-600 hover:bg-gray-50 border-l-4 border-transparent transition">
          <i class="fas fa-gavel mr-2"></i> My Disputes
        </a>
        <a href="/verification" class="block px-6 py-4 bg-primary-50 text-primary-600 font-bold border-l-4 border-primary-600">
          <i class="fas fa-id-card mr-2"></i> Verification
        </a>
      </nav>

      <div class="card bg-blue-50 dark:bg-blue-900/10 border-blue-200">
        <h4 class="font-bold text-blue-900 dark:text-blue-400 mb-2 flex items-center">
          <i class="fas fa-info-circle mr-2"></i> Why verify?
        </h4>
        <ul class="text-xs text-blue-800 dark:text-blue-300 space-y-2 list-disc pl-4">
          <li>Increased withdrawal limits</li>
          <li>Instant internal transfers</li>
          <li>Access to institutional features</li>
          <li>Enhanced account protection</li>
        </ul>
      </div>
    </div>

    <div class="md:col-span-2 space-y-8">
      <div class="card">
        <div class="flex items-center justify-between mb-8">
          <h3 class="text-xl font-bold text-gray-900 dark:text-white">Verification Status</h3>
          <span class="px-3 py-1 rounded-full text-xs font-bold uppercase <%= user.verificationStatus === 'Verified' ? 'bg-green-100 text-green-700' : (user.verificationStatus === 'Unverified' ? 'bg-gray-100 text-gray-700' : 'bg-yellow-100 text-yellow-700') %>">
            <%= user.verificationStatus %>
          </span>
        </div>

        <% if (locals.reason === 'verification_required') { %>
          <div class="mb-6 bg-blue-50 dark:bg-blue-900/20 border-l-4 border-blue-400 p-4 text-sm text-blue-700 dark:text-blue-400">
            <i class="fas fa-info-circle mr-2"></i> Action Required: Please complete verification to proceed with your transaction.
          </div>
        <% } %>

        <% if (locals.error) { %>
          <div class="mb-6 bg-red-50 dark:bg-red-900/20 border-l-4 border-red-400 p-4 text-sm text-red-700 dark:text-red-400">
             <%= error %>
          </div>
        <% } %>

        <% if (user.verificationStatus === 'Unverified') { %>
          <div class="space-y-8">
            <% if (!verification || !verification.feePaid) { %>
              <div class="p-6 bg-gray-50 dark:bg-gray-700/30 rounded-2xl border border-gray-100 dark:border-gray-700">
                <div class="flex items-start mb-4">
                  <div class="w-10 h-10 bg-primary-600 text-white rounded-full flex items-center justify-center font-bold mr-4 shrink-0">1</div>
                  <div>
                    <h4 class="font-bold text-lg">Pay Activation Fee</h4>
                    <p class="text-sm text-muted mt-1">A one-time fully refundable fee of $1.00 is required to prevent fraudulent activity.</p>
                  </div>
                </div>
                <form action="/verification/pay-fee" method="POST">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button type="submit" class="btn btn-primary w-full py-4 text-lg">Pay $1.00 from Wallet</button>
                </form>
              </div>
            <% } else { %>
              <div class="p-6 bg-gray-50 dark:bg-gray-700/30 rounded-2xl border border-gray-100 dark:border-gray-700">
                <div class="flex items-start mb-6">
                  <div class="w-10 h-10 bg-primary-600 text-white rounded-full flex items-center justify-center font-bold mr-4 shrink-0">2</div>
                  <div>
                    <h4 class="font-bold text-lg text-gray-900 dark:text-white">Submit Identity Documents</h4>
                    <p class="text-sm text-muted mt-1">Please upload a clear scan of your National ID or Passport.</p>
                  </div>
                </div>
                <form action="/verification/submit-kyc" method="POST" enctype="multipart/form-data" class="space-y-6">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <div class="border-2 border-dashed border-gray-300 dark:border-gray-600 rounded-2xl p-8 text-center hover:border-primary-500 transition cursor-pointer bg-white dark:bg-gray-800">
                    <input type="file" name="id_doc" required id="file-upload" class="hidden">
                    <label for="file-upload" class="cursor-pointer">
                       <i class="fas fa-cloud-upload-alt text-4xl text-gray-300 mb-4 block"></i>
                       <span class="text-primary-600 font-bold block mb-1">Click to upload document</span>
                       <span class="text-xs text-muted">Supports PNG, JPG or PDF up to 5MB</span>
                    </label>
                  </div>
                  <button type="submit" class="btn btn-primary w-full py-4 text-lg">Submit for Approval</button>
                </form>
              </div>
            <% } %>
          </div>
        <% } else if (user.verificationStatus === 'Pending') { %>
          <div class="text-center py-12">
            <div class="w-20 h-20 bg-yellow-100 dark:bg-yellow-900/30 text-yellow-600 rounded-full flex items-center justify-center text-3xl mx-auto mb-6">
              <i class="fas fa-clock"></i>
            </div>
            <h3 class="text-2xl font-bold mb-2">Review in Progress</h3>
            <p class="text-muted max-w-sm mx-auto">Your documents have been submitted and are currently being reviewed by our compliance team. This typically takes 24 hours.</p>
          </div>
        <% } else if (user.verificationStatus === 'Verified') { %>
          <div class="text-center py-12">
            <div class="w-20 h-20 bg-green-100 dark:bg-green-900/30 text-green-600 rounded-full flex items-center justify-center text-3xl mx-auto mb-6 shadow-xl">
              <i class="fas fa-check-circle"></i>
            </div>
            <h3 class="text-2xl font-bold mb-2">Account Fully Verified</h3>
            <p class="text-muted max-w-sm mx-auto">Thank you for verifying your identity. You now have full access to all features of <%= process.env.COMPANY_NAME %>.</p>
            <div class="mt-8 flex justify-center gap-4">
               <a href="/dashboard" class="btn btn-primary">Go to Dashboard</a>
               <a href="/withdraw" class="btn" style="background: var(--bg-alt); color: var(--text);">Make a Withdrawal</a>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</div>
